[project]
name = "cert-reorder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
  'pyopenssl',
]
[project.optional-dependencies]
dev = [
    "ruff",
    "pre-commit",
    "safety",
]
